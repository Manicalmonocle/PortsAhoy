<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The window behind the engine while it starts. Matches the game's own
         background so launching does not flash a colour the game never uses.

         When the ManicalGaming logo arrives it goes here, as a bitmap item
         below — centred, and it wants to be a drawable of its own rather than
         a scaled icon. -->
    <item android:drawable="@color/splash_background" />
</layer-list>
